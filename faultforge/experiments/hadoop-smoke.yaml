name: hadoop-smoke
runs:
  - name: nw-delay-100ms
    issue_id: ""
    system: hadoop
    benchmark: terasort
    oracle: oracles/hadoop-smoke.yaml
    strategy: exhaustive
    max_trials: 1
    max_faults_per_trial: 1
    nodes:
      - namenode
    fault_models:
      - nw
    magnitudes_ms:
      - 100
    start_times_s:
      - 5
    durations_s:
      - 10
