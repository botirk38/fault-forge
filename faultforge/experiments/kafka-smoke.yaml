name: kafka-smoke
runs:
  - name: nw-delay-100ms
    issue_id: ""
    system: kafka
    benchmark: openmsg
    oracle: oracles/kafka-smoke.yaml
    strategy: exhaustive
    max_trials: 1
    max_faults_per_trial: 1
    nodes:
      - kafka1
    fault_models:
      - nw
    magnitudes_ms:
      - 100
    start_times_s:
      - 5
    durations_s:
      - 10
