issue:
  id: "DEPFAST-SMOKE-1"
  system: "depfast"
  title: "Smoke test — request timeout under network delay"
  source: "synthetic"
  category: "availability"

oracle:
  invalid_if:
    any:
      - file: "info"
        contains: "docker-compose up failed"
      - file: "info"
        contains: "No such container"

  reproduced_if:
    any:
      - file: "info"
        contains: "timeout"
      - file: "info"
        contains: "failed"
      - file: "depfast_misc"
        contains: "error"
